# coding-session

React hooks practice. A counter built with `useState`, with a `useEffect` that syncs the
current count into the document title.

Small on purpose — this was a focused exercise on hook behaviour and re-render timing,
not a product.

## Stack

React, Create React App.

## Running it

```bash
npm install
npm start
```
